extends layout

block content
    - for (var i = count -1 ; i >= 0; i--)
      .widget
        h1 #{messages[i].header.from[0]}
        h3 #{messages[i].header.subject[0]}
        p  #{messages[i].text}
